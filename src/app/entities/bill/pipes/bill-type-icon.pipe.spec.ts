import { BillTypeIconPipe } from './bill-type-icon.pipe';

describe('BillTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new BillTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
