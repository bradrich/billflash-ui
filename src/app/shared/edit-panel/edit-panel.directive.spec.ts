import { EditPanelDirective } from './edit-panel.directive';

describe('EditPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new EditPanelDirective();
    expect(directive).toBeTruthy();
  });
});
