import { ElementPositionPipe } from './element-position.pipe';

describe('ElementPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new ElementPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
