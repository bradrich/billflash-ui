import { NaturalTypingPipe } from './natural-typing.pipe';

describe('NaturalTypingPipe', () => {
  it('create an instance', () => {
    const pipe = new NaturalTypingPipe();
    expect(pipe).toBeTruthy();
  });
});
