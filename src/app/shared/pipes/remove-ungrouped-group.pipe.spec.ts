import { RemoveUngroupedGroupPipe } from './remove-ungrouped-group.pipe';

describe('RemoveUngroupedGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveUngroupedGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
